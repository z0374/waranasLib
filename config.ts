export interface WaranasConfig {
  entry_point: string;
  default_lang: string;
  asset_url: string;
}

export const config: WaranasConfig = {
  entry_point: "",
  default_lang: "pt-BR",
  asset_url: "",
};
